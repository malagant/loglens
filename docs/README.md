# LogLens docs

Lands with the first feature-complete alpha:

- `demo.gif` / `demo.cast` — asciinema or VHS recording of the canonical use case
- `architecture.md` — adapter interface, merge pipeline, filter DSL grammar
- `installation.md` — Homebrew / Scoop / Linux package manager flows

The README links to `demo.gif`; the placeholder lives next to this file as `demo.gif.placeholder` until the real recording exists.
